import styled from 'styled-components';
import { Divider } from 'antd';

export const NoContentFrame = styled.div`
  display: flex;
  flex-direction: row;
`;

export const PaginationFrame = styled.div`
  text-align: center;
`;

export const StyledDivider = styled(Divider)`
  margin: 4px 0;
`;
