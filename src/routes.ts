const routes = {
  home: '/',
  components: '/Components',
  logIn: '/log-in',
  register: '/register',
  editProfile: '/edit-profile',
  doctorProcedures: '/my-procedures',
};

export default routes;
