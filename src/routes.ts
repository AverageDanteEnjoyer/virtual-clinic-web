const routes = {
  home: '/',
  components: '/Components',
  logIn: '/log-in',
  register: '/register',
  editProfile: '/edit-profile',
  makeAppointment: '/make-appointment',
};

export default routes;
